<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pet Care : Pet Detail</title>
  </head>
  <body>
    <a href="/"><h1>Pet Care</h1></a>
    <div>
      <% if(pets == null){ %> <% for(var i=0; i< pets.length; i++) { %>
      <div class="container">
        <h3><span><%= pets[i].id %>.</span> <%= pets[i].name %></h3>
        <ul>
          <li>age: <%= pets[i].age%></li>
          <li>birth: <%= pets[i].birth%></li>
          <li>type: <%= pets[i].type%></li>
          <li>type_details: <%= pets[i].type_details%></li>
        </ul>
      </div>
      <div><a href="/pet/edit/<%= pets[i].id %>">edit &rarr;</a></div>
      <% }}else { %>
      <li>Sorry Nothing found.</li>
      <% } %>
    </div>
  </body>
</html>
